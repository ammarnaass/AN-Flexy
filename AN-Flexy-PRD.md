# PRD — AN Flexy

| | |
|---|---|
| **المنتج** | AN Flexy — إدارة محل تعبئة الرصيد (فليكسي) |
| **النوع** | تطبيق سطح مكتب، يعمل بدون إنترنت (Offline-first) |
| **الإصدار** | v1.0 — مسودة |
| **التاريخ** | 2026-09-19 |
| **التقنيات** | Electron + Vite + React + TypeScript + Tailwind + Drizzle ORM + SQLite + npm |

---

## 1. نظرة عامة

### المشكلة
صاحب محل الفليكسي يسجّل المبيعات والشحنات والديون في دفاتر ورقية أو ذاكرته. النتيجة: أخطاء في الرصيد المتبقي، ديون منسية، ولا يعرف ربحه الحقيقي اليومي.

### الرؤية
تطبيق سريع وبسيط على حاسوب المحل: يسجّل كل عملية بيع في ثوانٍ، ويعرف رصيد كل متعامل وديون كل زبون وأرباح اليوم في أي لحظة.

### الأهداف القابلة للقياس

| # | الهدف | المعيار |
|---|---|---|
| G1 | سرعة تسجيل البيع | ≤ 5 ثوانٍ بالكيبورد فقط |
| G2 | دقة الرصيد | رصيد كل متعامل يطابق الحساب اليدوي 100% |
| G3 | سلامة البيانات | نسخة احتياطية تلقائية يوميًا، ولا فقدان بيانات عند انقطاع الكهرباء |
| G4 | التقارير | تقرير اليوم (المبيعات، الأرباح، الديون) بنقرة واحدة |

### خارج النطاق في v1
- **الإرسال الفعلي للتعبئة** (SMS / USSD / موديم / API). التطبيق **يسجّل** العمليات فقط. ليس لدي معلومات مؤكدة عن واجهات رسمية متاحة للموزّعين، لذلك لا أعد بها.
- المزامنة السحابية وتعدد الأجهزة.
- تطبيق موبايل (استُبدل بتكامل تليغرام، القسم 5).
- الفوترة الضريبية.

---

## 2. الافتراضات (عدّلها إن لم تناسبك)

1. محل واحد، وجهاز واحد يشغّل التطبيق.
2. نظام التشغيل المستهدف للمستخدم النهائي: **Windows** (مع بناء Linux للتطوير والاختبار).
3. العملة: الدينار الجزائري (DA)، والمبالغ تُخزَّن بالسنتيم (×100) كأعداد صحيحة.
4. المتعاملون الافتراضيون: Mobilis، Djezzy، Ooredoo، ويمكن إضافة غيرهم.
5. الواجهة بالعربية (RTL) في v1، والفرنسية لاحقًا.
6. الربح = مبلغ البيع × نسبة هامش المتعامل وقت البيع.

---

## 3. المستخدمون والصلاحيات

| الوظيفة | Admin (المالك) | Cashier (البائع) |
|---|:-:|:-:|
| تسجيل بيع | ✅ | ✅ |
| تسجيل دفعة دين | ✅ | ✅ |
| عرض رصيد المتعاملين | ✅ | ✅ |
| إلغاء بيع (Void) | ✅ | ❌ |
| شحن المخزون / تسوية الرصيد | ✅ | ❌ |
| المتعاملون والهوامش | ✅ | ❌ |
| تقارير الأرباح الكاملة | ✅ | ❌ (مبيعاته فقط، بدون أرباح) |
| النسخ الاحتياطي والاسترجاع | ✅ | ❌ |
| المستخدمون، الإعدادات، تليغرام | ✅ | ❌ |

---

## 4. المتطلبات الوظيفية

### الأولويات
- **P0:** ضرورية لـ v1.0.
- **P1:** تُضاف قبل الإصدار إن سمح الوقت، وإلا في v1.1.
- **P2:** لاحقًا.

### FR-01 — الدخول والمستخدمون (P0)
- شاشة دخول باسم المستخدم + PIN (4–6 أرقام).
- أول تشغيل: معالج يُنشئ حساب المالك ويضبط المتعاملين.
- PIN يُخزَّن مُجزَّءًا (scrypt + salt)، ولا يُخزَّن نصًا.
- قفل تلقائي بعد مدة خمول قابلة للضبط.

**معايير القبول:** 5 محاولات خاطئة → قفل 30 ثانية. Cashier لا يرى أي شاشة أو زر لا يملك صلاحيته، والتحقق يتم أيضًا في Main process وليس في الواجهة فقط.

### FR-02 — المتعاملون (P0)
- إضافة/تعديل/تعطيل متعامل (اسم، هامش بالنقاط الأساسية، حد تنبيه الرصيد المنخفض).
- تعديل الهامش لا يؤثر على المبيعات السابقة، فالربح يُحفظ لحظة البيع.

**معايير القبول:** لا يمكن حذف متعامل له عمليات، بل تعطيله فقط.

### FR-03 — المخزون (P0)
- **شحن رصيد:** المتعامل، المبلغ المدفوع، الرصيد المستلم، ملاحظة.
- **تسوية:** تصحيح الرصيد عند اختلافه عن الرصيد الفعلي لدى المتعامل (قيمة موجبة أو سالبة + سبب إلزامي).
- عرض الرصيد المتبقي لكل متعامل، وسجل الشحنات.

**معايير القبول:** الرصيد المتبقي يُحسب من السجلات ولا يُخزَّن كحقل قابل للتعديل. كل تسوية تُسجَّل في سجل التدقيق.

### FR-04 — البيع (P0)
- الحقول: المتعامل، رقم الهاتف، المبلغ، المدفوع (افتراضيًا = المبلغ)، الزبون (اختياري).
- إن كان المدفوع < المبلغ فالزبون إلزامي والفرق يُسجَّل دينًا.
- التحقق: الرقم 10 أرقام يبدأ بـ 05/06/07، والمبلغ أكبر من صفر.
- منع البيع عند نقص رصيد المتعامل، مع رسالة توضح الرصيد المتاح. (إعداد للمالك: السماح بالرصيد السالب.)
- اختصارات لوحة المفاتيح: أرقام سريعة للمبالغ الشائعة (50/100/200/500)، `Enter` للتأكيد.

**معايير القبول:** العملية كلها (فحص الرصيد + الحفظ + التدقيق) داخل **Transaction واحدة**. محاولتان متزامنتان لا تجعلان الرصيد سالبًا.

### FR-05 — إلغاء بيع (P0)
- المالك فقط. الإلغاء **soft-void** (`voided_at` + سبب)، ولا حذف فعلي.
- الملغى يُستبعد من الرصيد والتقارير والديون، ويبقى ظاهرًا في السجل.

### FR-06 — الزبائن (P0)
- إضافة/تعديل زبون (اسم، هاتف اختياري).
- ملف الزبون: مشترياته، ديونه، دفعاته.
- بحث سريع بالاسم أو الرقم أثناء البيع.

### FR-07 — الديون والدفعات (P0)
- تسجيل دفعة على حساب زبون (كاملة أو جزئية).
- الدفعة تُخصم من إجمالي دين الزبون (بدون ربط بفاتورة محددة في v1).
- قائمة الديون مرتبة بالأكبر، مع تاريخ أقدم دين.

**معايير القبول:** لا يمكن تسجيل دفعة أكبر من دين الزبون الحالي.

### FR-08 — لوحة التحكم (P0)
- بطاقة لكل متعامل: الرصيد المتبقي (بلون تحذيري عند بلوغ الحد الأدنى).
- مبيعات اليوم، أرباح اليوم (للمالك)، إجمالي الديون.
- آخر 10 عمليات.

### FR-09 — التقارير (P0)
- تقرير يومي وشهري وفترة مخصصة: المبيعات، الأرباح، حسب المتعامل، حسب البائع.
- تقرير الديون.
- **P1:** تصدير PDF / Excel.

### FR-10 — النسخ الاحتياطي (P0)
- تصدير نسخة يدوية إلى مسار يختاره المستخدم.
- استرجاع من ملف (مع تأكيد، ونسخة أمان تلقائية قبل الاسترجاع).
- **P1:** نسخة تلقائية يوميًا واحتفاظ بآخر N نسخة.

**معايير القبول:** النسخ يتم بـ `better-sqlite3 .backup()` وليس بنسخ ملف حي وقت الكتابة.

### FR-11 — الإعدادات (P0)
- اسم المحل، المتعاملون، المستخدمون، حد التنبيه، مدة القفل، مسار النسخ الاحتياطي.

### FR-12 — تكامل تليغرام (P1)
انظر القسم 5.

### FR-13 — طباعة وصل بيع (P1)
- وصل مختصر (اسم المحل، التاريخ، المتعامل، الرقم، المبلغ، المدفوع/المتبقي).
- ليس لدي معلومات عن الطابعة التي ستستخدمها؛ قد يتطلب دعمها للطابعات الحرارية اختبارًا على الجهاز الفعلي.

### FR-14 — اقتراح المتعامل تلقائيًا (P1)
- اقتراح المتعامل من بادئة الرقم (05 / 06 / 07) مع إمكانية التغيير يدويًا. **هذا افتراض عام أحتاج أن تتأكد منه بنفسك** قبل الاعتماد عليه.

---

## 5. تكامل تليغرام (P1)

**الهدف:** متابعة المحل من الهاتف: إشعارات واستعلامات سريعة.

### المتطلبات
1. **الربط:** يولّد التطبيق كودًا من 6 أرقام صالحًا 5 دقائق. يرسل المالك `/start CODE` للبوت فيُحفظ معرّفه (Telegram user ID) في القائمة المسموحة.
2. **الأوامر:** `/balance` (أرصدة المتعاملين)، `/today` (مبيعات وأرباح اليوم)، `/debts` (الديون).
3. **الإشعارات:** انخفاض الرصيد عن الحد، وملخص يومي في ساعة محددة.
4. **الصلاحيات:** يرد البوت على المعرّفات المربوطة فقط، ويتجاهل غيرها بصمت.
5. **الإعدادات:** تفعيل/إيقاف، إدخال التوكن، عرض الأجهزة المربوطة وفصلها.
6. **P2:** تسجيل بيع من تليغرام.

### القرارات التقنية
- البوت يعمل داخل **Main process** بمكتبة **grammY** وأسلوب **Long Polling** (لا سيرفر ولا دومين ولا منفذ مفتوح).
- التوكن يُشفَّر بـ `safeStorage` قبل حفظه في جدول `settings`.
- فشل الاتصال بتليغرام **لا يؤثر** على بقية التطبيق.

### القيود (يجب أن يعرفها المستخدم)
- **يعمل البوت فقط أثناء تشغيل التطبيق واتصال الجهاز بالإنترنت.** الإشعارات الفائتة لا تُعاد.
- كل ما يُرسل يمر عبر سيرفرات تليغرام: لا تُرسل أرقام هواتف الزبائن كاملة، والاكتفاء بالاسم والمبلغ.
- من يملك التوكن يتحكم بالبوت: يُنصح بتعطيل إضافة البوت للمجموعات عبر @BotFather (`/setjoingroups` → Disable).
- إن حجبت الشبكة تليغرام يتوقف التكامل فقط.

---

## 6. المتطلبات غير الوظيفية

| المجال | المتطلب |
|---|---|
| الأداء | بدء التشغيل ≤ 3 ثوانٍ، وفتح أي شاشة ≤ 300ms مع 100,000 عملية |
| الاعتمادية | WAL + Transactions لكل عملية تمس الرصيد؛ لا فقدان بيانات عند انقطاع الكهرباء |
| الأمان | `contextIsolation: true`، `nodeIntegration: false`، `sandbox: true`، CSP صارم، منع التنقل والنوافذ الخارجية |
| العمل دون إنترنت | كل الوظائف الأساسية بدون شبكة، والخطوط والأصول محلية (لا CDN) |
| RTL | كل الواجهة من اليمين لليسار، مع أرقام لاتينية منسّقة، وخط عربي مضمّن |
| قابلية الصيانة | TypeScript صارم، طبقة Services منفصلة عن IPC، وترحيلات قاعدة بيانات مُسجَّلة |
| التدقيق | سجل لكل: بيع، إلغاء، شحن، تسوية، دفعة، تعديل هامش، دخول فاشل |

---

## 7. البنية التقنية

### 7.1 الحزمة
| الطبقة | الاختيار | ملاحظة |
|---|---|---|
| الغلاف | Electron | |
| البناء | Vite عبر `electron-vite` | scaffold رسمي بقالب `react-ts` |
| الواجهة | React 18/19 + TypeScript + Tailwind | |
| قاعدة البيانات | SQLite عبر `better-sqlite3` | مكتبة native، تحتاج `install-app-deps` |
| ORM | Drizzle ORM + `drizzle-kit` | ترحيلات SQL مُولَّدة |
| التحقق | zod | لكل مدخلات IPC |
| التغليف | electron-builder | NSIS للويندوز، AppImage/rpm للينكس |
| الاختبار | Vitest (+ Playwright اختياري) | قاعدة `:memory:` للاختبارات |
| تليغرام | grammY | P1 |

### 7.2 هيكل المشروع
```
an-flexy/
├─ src/
│  ├─ main/
│  │  ├─ index.ts            # نافذة Electron + دورة الحياة
│  │  ├─ db/
│  │  │  ├─ schema.ts
│  │  │  ├─ client.ts        # فتح القاعدة + pragma + migrate
│  │  │  └─ migrations/      # ناتج drizzle-kit
│  │  ├─ services/           # منطق العمل (sales, stock, debts, reports, backup, auth)
│  │  ├─ ipc/                # handlers رقيقة + zod
│  │  └─ telegram/           # P1
│  ├─ preload/index.ts       # contextBridge
│  └─ renderer/              # React
├─ drizzle.config.ts
└─ electron-builder.yml
```

### 7.3 قواعد البنية
1. القاعدة **تُفتح وتُستعلم في Main process فقط**؛ الواجهة لا ترى SQLite أبدًا.
2. الواجهة → `window.api.*` (preload) → `ipcMain.handle` → Service → Drizzle.
3. كل handler: يتحقق من المدخلات بـ zod، ثم من صلاحية المستخدم الحالي (محفوظ في Main)، ثم يستدعي Service، ويعيد `{ ok, data | error }`.
4. الخدمات لا تعرف Electron (تستقبل `db` كمعامل) لتُختبر بقاعدة `:memory:`.
5. ملف القاعدة في `app.getPath('userData')`، مع `journal_mode = WAL` و`foreign_keys = ON`.
6. الترحيلات تُشغَّل تلقائيًا عند بدء التطبيق، وتُنسخ مجلداتها عبر `extraResources` في النسخة المغلَّفة.

---

## 8. نموذج البيانات (Drizzle)

```ts
// src/main/db/schema.ts
import { sqliteTable, integer, text, index } from 'drizzle-orm/sqlite-core'
import { sql } from 'drizzle-orm'

const now = sql`(CURRENT_TIMESTAMP)`

export const users = sqliteTable('users', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  name: text('name').notNull().unique(),
  role: text('role', { enum: ['admin', 'cashier'] }).notNull(),
  pinHash: text('pin_hash').notNull(),
  active: integer('active', { mode: 'boolean' }).notNull().default(true),
  createdAt: text('created_at').notNull().default(now),
})

export const operators = sqliteTable('operators', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  name: text('name').notNull().unique(),           // Mobilis, Djezzy, Ooredoo
  marginBp: integer('margin_bp').notNull().default(0), // 350 = 3.5%
  lowBalanceAt: integer('low_balance_at').notNull().default(0),
  active: integer('active', { mode: 'boolean' }).notNull().default(true),
})

export const stockEntries = sqliteTable('stock_entries', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  operatorId: integer('operator_id').notNull().references(() => operators.id),
  type: text('type', { enum: ['purchase', 'adjustment'] }).notNull().default('purchase'),
  costAmount: integer('cost_amount').notNull().default(0),  // سنتيم مدفوعة
  creditAmount: integer('credit_amount').notNull(),         // سنتيم رصيد (سالب مسموح للتسوية فقط)
  note: text('note'),
  userId: integer('user_id').references(() => users.id),
  createdAt: text('created_at').notNull().default(now),
})

export const customers = sqliteTable('customers', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  name: text('name').notNull(),
  phone: text('phone'),
  note: text('note'),
  createdAt: text('created_at').notNull().default(now),
})

export const sales = sqliteTable('sales', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  operatorId: integer('operator_id').notNull().references(() => operators.id),
  customerId: integer('customer_id').references(() => customers.id),
  targetPhone: text('target_phone').notNull(),
  amount: integer('amount').notNull(),
  paidAmount: integer('paid_amount').notNull(),
  profit: integer('profit').notNull(),               // لقطة وقت البيع
  userId: integer('user_id').notNull().references(() => users.id),
  voidedAt: text('voided_at'),
  voidReason: text('void_reason'),
  createdAt: text('created_at').notNull().default(now),
}, (t) => [
  index('sales_created_idx').on(t.createdAt),
  index('sales_customer_idx').on(t.customerId),
  index('sales_operator_idx').on(t.operatorId),
])

export const payments = sqliteTable('payments', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  customerId: integer('customer_id').notNull().references(() => customers.id),
  amount: integer('amount').notNull(),
  userId: integer('user_id').references(() => users.id),
  createdAt: text('created_at').notNull().default(now),
})

export const settings = sqliteTable('settings', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),
})

export const auditLog = sqliteTable('audit_log', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  userId: integer('user_id').references(() => users.id),
  action: text('action').notNull(),
  details: text('details'),
  createdAt: text('created_at').notNull().default(now),
})
```

### قواعد الحساب (مصدر الحقيقة)
- **رصيد المتعامل** = Σ `stock_entries.credit_amount` − Σ `sales.amount` (حيث `voided_at IS NULL`).
- **دين الزبون** = Σ (`sales.amount` − `sales.paid_amount`) للمبيعات غير الملغاة − Σ `payments.amount`.
- **ربح البيع** = `round(amount × margin_bp / 10000)` ويُحفظ في `sales.profit`.
- **ربح اليوم** = Σ `profit` للمبيعات غير الملغاة.

### ملاحظات مهمة
- الطوابع الزمنية تُخزَّن **UTC**. في تقارير اليوم استخدم `date(created_at, 'localtime')` وإلا ستظهر مبيعات ما بعد منتصف الليل في يوم خاطئ.
- المبالغ كلها أعداد صحيحة بالسنتيم، ولا `float` في أي مكان.
- لا حذف فعلي للمبيعات ولا الدفعات: الإلغاء المنطقي فقط.

---

## 9. الشاشات وتدفقاتها

1. **الدخول** — اسم المستخدم + PIN.
2. **لوحة التحكم** — بطاقات الأرصدة، مؤشرات اليوم، آخر العمليات.
3. **بيع جديد** (الشاشة الأهم) — تركيز تلقائي على حقل الرقم → المتعامل → المبلغ → `Enter`. تظهر رسالة نجاح ويُفرَّغ النموذج للعملية التالية.
4. **المخزون** — الأرصدة، شحن جديد، تسوية، السجل.
5. **الزبائن والديون** — القائمة، ملف الزبون، تسجيل دفعة.
6. **التقارير** — اختيار الفترة، جداول، تصدير (P1).
7. **الإعدادات** — المتعاملون، المستخدمون، النسخ الاحتياطي، تليغرام (P1).

### مبادئ الواجهة
- تصميم لشخص مشغول أمام زبون: أزرار كبيرة، اختصارات، لا نوافذ منبثقة غير ضرورية.
- خط عربي مضمّن محليًا (مثل Cairo أو Tajawal). لا تحميل من الإنترنت.
- استخدم خصائص Tailwind المنطقية (`ms-*`، `me-*`، `ps-*`، `pe-*`) بدل `ml/mr/pl/pr` لتعمل مع RTL.

---

## 10. الاختبار والجودة

- **اختبارات وحدة (Vitest)** على الخدمات بقاعدة `:memory:`: حساب الرصيد، الديون، الأرباح، منع الرصيد السالب، الإلغاء، تسوية المخزون.
- **اختبار تزامن:** بيعان متتاليان سريعان لا يتجاوزان الرصيد.
- **اختبار ترحيلات:** تشغيل كل الترحيلات على قاعدة فارغة، وعلى نسخة من قاعدة v سابقة قبل كل إصدار.
- **اختبار استرجاع النسخ الاحتياطي** فعليًا، لا افتراضًا.
- **اختبار ميداني** أسبوع على الأقل بجانب الدفتر الورقي قبل الاعتماد الكامل.

---

## 11. التغليف والتوزيع

- `electron-builder`: هدف NSIS (Windows) وAppImage/rpm (Linux).
- بناء نسخة Windows من Fedora يحتاج Wine وقد يكون متعبًا. الأنسب استخدام **GitHub Actions** بـ runner ويندوز.
- تحقق أن `better-sqlite3` يُبنى لإصدار Electron الصحيح (`postinstall: electron-builder install-app-deps`) وأن ملف `.node` خارج أرشيف asar (`asarUnpack`).
- `extraResources` لمجلد الترحيلات.
- التوقيع الرقمي (Code Signing) غير مطلوب للتشغيل، لكن غيابه يُظهر تحذير SmartScreen في ويندوز عند أول تشغيل.
- التحديث التلقائي (`electron-updater`) **P2** لأنه يحتاج مكانًا لاستضافة الإصدارات.

---

## 12. المخاطر

| الخطر | الأثر | المعالجة |
|---|---|---|
| فقدان ملف القاعدة | فقدان كل السجلات | نسخ احتياطي تلقائي + تنبيه إن لم يُنسخ منذ N أيام |
| فشل بناء `better-sqlite3` على ويندوز | تعطل التغليف | تجربة البناء في الأسبوع الأول لا الأخير |
| اختلاف الرصيد عن الرصيد الفعلي لدى المتعامل | ثقة أقل بالتطبيق | ميزة التسوية مع سجل تدقيق |
| تسريب توكن البوت | تحكم غريب بالبوت | تشفير `safeStorage`، وعدم حفظه في Git أو الكود |
| توقع المستخدم أن التطبيق يرسل التعبئة | خيبة أمل | توضيح صريح في الواجهة والوثائق أنه يسجّل فقط |
| تقدير الوقت | تأخر | التقديرات لمطوّر واحد، وترتيب القص في وثيقة الخطة |

---

## 13. أسئلة مفتوحة

1. هل تريد لاحقًا ربطًا فعليًا بالإرسال؟ إن نعم فاختيار الطريقة يؤثر على نموذج البيانات (حالة العملية: معلقة / نجحت / فشلت).
2. هل يوجد أكثر من بائع على نفس الجهاز يوميًا؟ (يؤثر على قفل الشاشة وتقارير البائع.)
3. هل تحتاج طباعة وصل من اليوم الأول، وما نوع الطابعة؟
4. هل يوجد متعاملون غير الثلاثة (بطاقات، إنترنت ADSL/4G، ألعاب)؟

---

## 14. ما بعد v1

- إرسال التعبئة الفعلي (حسب الطريقة المتاحة).
- تسجيل بيع من تليغرام.
- تحديث تلقائي.
- الفرنسية.
- ربط الدفعات بفواتير محددة.
- تكلفة الرصيد بالمتوسط المرجّح بدل الهامش الثابت.
- نسخ احتياطي مشفَّر إلى التخزين السحابي (اختياري).
